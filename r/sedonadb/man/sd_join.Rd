% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dataframe-join.R
\name{sd_join}
\alias{sd_join}
\alias{sd_left_join}
\alias{sd_right_join}
\alias{sd_inner_join}
\alias{sd_full_join}
\alias{sd_semi_join}
\alias{sd_anti_join}
\alias{sd_cross_join}
\title{Join two SedonaDB DataFrames}
\usage{
sd_join(
  x,
  y,
  by = NULL,
  join_type = "inner",
  select = sd_join_select_default(),
  keep = NULL
)

sd_left_join(x, y, by = NULL, select = sd_join_select_default(), keep = NULL)

sd_right_join(x, y, by = NULL, select = sd_join_select_default(), keep = NULL)

sd_inner_join(x, y, by = NULL, select = sd_join_select_default(), keep = NULL)

sd_full_join(x, y, by = NULL, select = sd_join_select_default(), keep = NULL)

sd_semi_join(x, y, by = NULL)

sd_anti_join(x, y, by = NULL)

sd_cross_join(x, y, select = sd_join_select_default())
}
\arguments{
\item{x}{The left dataframe}

\item{y}{The right dataframe}

\item{by}{Join specification. One of:
\itemize{
\item A \code{sedonadb_join_by} object from \code{\link[=sd_join_by]{sd_join_by()}}
\item A \code{sedonadb_join_by} object from \code{\link[=sd_join_intersects]{sd_join_intersects()}},
\code{\link[=sd_join_contains]{sd_join_contains()}}, \code{\link[=sd_join_within]{sd_join_within()}}, \code{\link[=sd_join_covers]{sd_join_covers()}},
\code{\link[=sd_join_coveredby]{sd_join_coveredby()}}, \code{\link[=sd_join_touches]{sd_join_touches()}}, \code{\link[=sd_join_crosses]{sd_join_crosses()}},
\code{\link[=sd_join_overlaps]{sd_join_overlaps()}}, or \code{\link[=sd_join_equals]{sd_join_equals()}}.
\item A character vector of column names to join on in both tables
\item A named character vector mapping left-table column names to
right-table column names, e.g. \code{c(x_val = "y_val")}
\item \code{NULL} for a natural join on columns with matching names
}}

\item{join_type}{The type of join to perform. One of "inner", "left", "right",
"full", "leftsemi", "rightsemi", "leftanti", "rightanti", "leftmark",
or "rightmark".}

\item{select}{Post-join column selection. One of
\itemize{
\item \code{NULL} for no modification, which may result in duplicate (unqualified)
column names. The column may still be
referred to with a qualifier in advanced usage using \code{\link[=sd_expr_column]{sd_expr_column()}}.
\item \code{\link[=sd_join_select_default]{sd_join_select_default()}} for dplyr-like behaviour (equi-join keys
or spatial join keys removed for inner/left joins, intersecting names
suffixed)
\item \code{\link[=sd_join_select]{sd_join_select()}} for a custom selection
}}

\item{keep}{Use \code{TRUE} to keep all key columns in an equijoin or spatial join.
This is only applied when using \code{\link[=sd_join_select_default]{sd_join_select_default()}} for left/inner
joins and right equijoins (full joins and right spatial joins always keep
spatial join keys).}
}
\value{
An object of class sedonadb_dataframe
}
\description{
Perform a join operation between two dataframes. Use \code{\link[=sd_join_by]{sd_join_by()}} to
specify join conditions using \code{x$column} and \code{y$column} syntax to
reference columns from the left and right tables respectively.
}
\examples{
df1 <- data.frame(x = letters[1:10], y = 1:10)
df2 <- data.frame(y = 10:1, z = LETTERS[1:10])
df1 |> sd_join(df2)

}
